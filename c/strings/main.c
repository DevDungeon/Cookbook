#include <strings.h>
#include <stdio.h>

int main(int argc, char* argv[]) {


	char text[6] = {'H', 'e', 'l', 'l', 'o', '\0'};

	int length = strlen(

	strcpy() //
	strcat() // concatenate
	strlen() // Length (excluding \0)
	strcmp() // compare 
	strchr() // find char
	strstr() // pointer to first occurence of str
}
