package net.electropunk.libs.files;

// Simple class for pulling specific bytes from a file

// for example: Pull first/last 8 bytes of this.zip (to check signatures)


/**
 *
 * @author John "NanoDano" Leon <nanodano@devdungeon.com>
 */
public class ByteExtractor {

    // if offset is -1, start from end? or add a bool startFromEnd?
    // public static byteArray getBytes(int numBytes, int offset, String filename) {
        // Example: Quickly grab first or last 8 bytes of a file to do a signature check
        // - pull first 512 bytes of an .ISO image to see if boot sector has been changed
    // }
}
